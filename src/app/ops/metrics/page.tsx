export default function Page() {
  return (
    <div className="border border-line bg-surface p-10">
      <p className="label">Not built yet</p>
      <h1 className="mt-2 font-cond text-2xl font-bold">metrics</h1>
      <p className="mt-3 max-w-prose text-[15px] text-ink2">
        See <code className="font-mono text-[13px]">docs/build-plan.md</code> for
        what this screen owns and the order to build it in. The domain logic it
        needs already exists in <code className="font-mono text-[13px]">src/lib</code>.
      </p>
    </div>
  );
}
